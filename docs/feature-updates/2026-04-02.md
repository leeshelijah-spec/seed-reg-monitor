# 2026-04-02

## 10:21 뉴스 트렌드 모듈 추가

- 네이버 뉴스 수집, 중복 병합, 분석, 운영 로그용 DB/서비스 계층을 추가
- 대시보드에 뉴스 KPI, 필터, 트렌드 차트, 경영 요약, 키워드 관리, 피드백 UI를 통합
- 샘플 데이터, 시드 스크립트, 아키텍처 문서, 실행 가이드를 정리하고 기본 테스트를 추가

## 읽기 전용 ngrok 공유

- `READ_ONLY_MODE`를 추가해 공유용 대시보드에서 쓰기 요청이 FastAPI 레이어에서 `403`으로 차단되도록 구성
- 읽기 전용 모드에서는 동기화, 리뷰 저장, 키워드 관리, 기사 피드백 같은 수정 UI를 숨기도록 정리
- `scripts/start_readonly_dashboard.ps1`을 추가해 읽기 전용 모드로 대시보드를 바로 실행할 수 있게 구성
- `scripts/start_ngrok_readonly_share.ps1`을 추가해 읽기 전용 트래픽 규칙으로 ngrok 공유를 시작할 수 있게 구성
- `config/ngrok-readonly-policy.example.yml`을 추가해 `GET`과 `HEAD` 이외 요청을 막는 예시 정책 제공
- 읽기 전용 페이지에서 쓰기 UI가 숨겨지고 쓰기 요청이 거부되는지 확인하는 회귀 테스트 추가

## 뉴스 대시보드 UI 및 문서 정리

- 상단 Hero 박스를 제거하고 `Seed Regulation Monitor` 하단에 설명 문구를 작게 배치
- `규제 동기화`, `뉴스 수집` 버튼을 더 작은 가로형 버튼으로 정리
- 규제 모니터링 현황과 뉴스 분석 KPI를 각각 2x2 카드 배치로 재구성
- 뉴스 섹션의 필터와 관리자용 키워드 관리를 상단 `필터` 버튼으로 여는 토글 패널로 통합
- 중요 기사 상위 리스트를 제거하고, 차트와 경영 요약, 운영 현황을 1x2 레이아웃으로 정리
- `.env.local` 로드 상태를 확인하고 네이버 API 설정이 실제 화면에 반영되도록 정리
- 업데이트 이력 관리 방식을 `releases`에서 `feature-updates` 중심으로 재정리

## 대시보드 그리드 축소 조정

- 규제 KPI 4개 카드의 높이와 폭을 줄여 더 촘촘한 2x2 그리드로 재배치
- 뉴스 KPI 4개 카드도 동일하게 축소된 2x2 그리드로 재배치
- `카테고리별 기사 건수`, `키워드별 언급 추이`, `경영 요약 패널`, `운영 현황`을 하나의 2x2 그리드로 재구성
- 960px 이하에서도 KPI, 차트, 운영 패널이 2열을 유지하도록 반응형 규칙을 조정
- 운영 현황 표에 중복 건수를 함께 표시해 뉴스 수집 성공과 신규 저장 여부를 구분할 수 있게 정리

## 미검토 중심 대시보드 재구성

- 규제 KPI와 뉴스 KPI를 최상단으로 올리고 `2x2 + 2x2` 형태의 개요 영역으로 재배치
- `카테고리별 기사 건수`, `키워드별 언급 추이`, `경영 요약 패널`, `운영 현황`이 그 다음 순서의 2x2 패널로 이어지도록 정리
- `최근 등록 목록`을 `미검토 목록`으로 변경하고 기본적으로 조치 이력이 없는 규제만 표시하도록 수정
- `미검토 기사 목록`을 추가해 기본적으로 피드백하지 않은 기사만 표시하고, 규제 목록과 기사 목록 모두 `전체목록보기` 토글을 지원
- 기사 피드백 저장 후 페이지 최상단이 아닌 기사 목록 섹션으로 다시 돌아오도록 리다이렉트 위치를 보정
- `분석 및 운영 현황` 4개 패널을 하나의 큰 박스 안에 감싸고, 미검토 보기 상태와 무관하게 누적 집계가 유지되도록 목록 필터와 집계 필터를 분리

## 실행 흐름 및 동기화 안정화

- `scripts/start_dashboard.ps1`과 `start_dashboard.cmd`를 추가해 편집 모드 서버를 바로 실행할 수 있게 정리
- 읽기 전용 스크립트에 포트 선점 확인과 `--reload-dir` 고정을 넣어 잘못된 감시 경로와 충돌 상황을 줄임
- 설정 로딩 순서를 조정해 실행 스크립트에서 지정한 환경변수가 `.env.local`보다 우선 적용되도록 보강
- `KOREAN_LAW_MCP_DIR` 상대경로가 비어 있거나 잘못되어도, 실제 빌드가 있는 `korean-law-mcp` 경로를 자동 탐색하도록 규제 동기화를 보강
- `korean_law_fetcher` 실행 실패 시 stderr와 경로 정보를 포함한 오류를 남기도록 정리하고, 경로 해석 테스트를 추가

## 파비콘 연결

- `app/favicon.ico`를 `/favicon.ico`로 직접 서빙하도록 연결
- 기본 템플릿에 favicon 링크를 추가해 브라우저 요청이 `404`로 남지 않도록 정리
